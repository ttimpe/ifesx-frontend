import { StopPlatformNumberPipe } from './stop-platform-number.pipe';

describe('StopPlatformNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new StopPlatformNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
