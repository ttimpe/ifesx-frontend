import { SpecialCharacter } from './special-character.model';

describe('SpecialCharacter', () => {
  it('should create an instance', () => {
    expect(new SpecialCharacter()).toBeTruthy();
  });
});
