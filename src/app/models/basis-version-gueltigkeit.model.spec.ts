import { BasisVersionGueltigkeit } from './basis-version-gueltigkeit.model';

describe('BasisVersionGueltigkeit', () => {
  it('should create an instance', () => {
    expect(new BasisVersionGueltigkeit()).toBeTruthy();
  });
});
