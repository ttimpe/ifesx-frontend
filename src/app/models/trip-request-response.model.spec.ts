import { TripRequestResponse } from './trip-request-response.model';

describe('TripRequestResponse', () => {
  it('should create an instance', () => {
    expect(new TripRequestResponse()).toBeTruthy();
  });
});
